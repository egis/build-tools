machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - npm install

test:
  override:
    - npm run dont-break && touch .local-success
  post:
    - test -f .local-success && (npm run semantic-release || true)
